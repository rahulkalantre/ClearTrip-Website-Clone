import React from "react";
import '../Styles/NavBarButton.css';

const NavBarButton = () => {
  return (
    <div>
      <button className="custom-button">Log in / Sign up</button>
    </div>
  );
};

export default NavBarButton;
